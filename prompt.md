1.1 检查看看代码有没有错误，有的话告诉我该改哪个函数的哪个地方，没问题后，写一份测试案例，测试所有函数的功能和所有报错，并在运行框中打印期望输出便于我对比

1.2 检查看看代码有没有错误，有的话告诉我该改哪个函数的哪个地方，没问题后，写一份测试案例，测试所有函数的功能，并在运行框中打印期望输出便于我对比

2. 双向链表的实现，C语言和C++各写出来，函数包括初始化，销毁，清空，获取元素（传位置），返回位序，获取前驱，获取后继，插入，删除，合并，打印(用于测试)可适当补充或增减上述所说的函数，按照书里面的要求来, 并写上C和C++的测试案例，测试所有功能和边界错误的处理


有几点问题：1. fix_mfset是先找根再重构树结构，对吗?所以应该先对每个结点应用一次吗，也就是调用时机在什么时候？2.你前面说配合 mix 和 fix，平均复杂度几乎可以看作常数级 $O(\alpha(n))$，但是mix时间复杂度为O(1)，得到的树深度不超过[log2(n)]+1，fix时间复杂度为常数阶($O(\alpha(n))$)，合起来不是二者相乘吗?3.最后解释一下书中143页的阿克曼函数那一块的内容，也包括拟逆、$O(\alpha(n))$，以及最后的$O(\alpha(n))$<=4是为什么，我想了解详细一些4.mix的到的树深度不超过[log2(n)]+1，树立面142页的归纳法，你讲的详细一些
```c

```

```cpp

```
