#include <stdio.h>
#include <stdlib.h>
#include "../../../Status.h"

/*
* 在解空间树中不仅找可行解，还要找使目标函数最优的那个。
* 回溯时维护当前最优值 best，若当前部分解的上界（通过松弛计算）已不优于 best，则剪去该子树（限界剪枝）。
* 典型如0-1背包用价值密度上界、旅行商问题用最小生成树下界进行剪枝，将指数级复杂度控制在可接受范围。
*/